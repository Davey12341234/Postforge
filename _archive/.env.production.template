# Production / Railway — copy keys into the web service Variables (or deploy/secrets.preview.env for deploy:check).
# Do not commit filled values. Source of required names: scripts/required-env.production.json

# Postgres — on Railway web service use Variable Reference: ${{ YourPostgresService.DATABASE_URL }}
DATABASE_URL=

NEXTAUTH_URL=https://your-app.up.railway.app
NEXTAUTH_SECRET=

ANTHROPIC_API_KEY=
# Optional: DALL·E, GPT Image, Whisper, OpenAI chat (if you use those features)
OPENAI_API_KEY=

STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_PRO=
STRIPE_PRICE_BUSINESS=
STRIPE_PRICE_ENTERPRISE=

ADMIN_REVENUE_SECRET=

# Optional — match NEXTAUTH_URL origin if the app builds absolute URLs
# APP_URL=https://your-app.up.railway.app
