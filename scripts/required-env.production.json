{
  "required": [
    "DATABASE_URL",
    "NEXTAUTH_URL",
    "NEXTAUTH_SECRET",
    "ANTHROPIC_API_KEY",
    "STRIPE_SECRET_KEY",
    "STRIPE_PUBLISHABLE_KEY",
    "STRIPE_WEBHOOK_SECRET",
    "STRIPE_PRICE_PRO",
    "STRIPE_PRICE_BUSINESS",
    "STRIPE_PRICE_ENTERPRISE",
    "ADMIN_REVENUE_SECRET"
  ],
  "notes": {
    "DATABASE_URL": "Postgres URL, or on Railway web service: ${{ Postgres.DATABASE_URL }}",
    "NEXTAUTH_URL": "https://your-app.up.railway.app — no trailing slash; not YOUR_DOMAIN_HERE",
    "NEXTAUTH_SECRET": "Random string (e.g. openssl rand -base64 32), not GENERATE_WITH_...",
    "STRIPE_PRICE_*": "Stripe Dashboard → Products → Price ID (price_...)"
  }
}
